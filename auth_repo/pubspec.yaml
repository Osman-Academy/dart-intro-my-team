name: auth_repo
description: Minimal auth/login logic for demo and tests.
publish_to: 'none'
version: 0.0.1
environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  crypto: ^3.0.2

dev_dependencies:
  test: ^1.21.0
