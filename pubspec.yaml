name: flutter_application_1
description: A small Flutter app demonstrating Clean Architecture, BLoC, DI, freezed, and REST integration.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  get_it: ^7.2.0
  http: ^0.13.5
  freezed_annotation: ^2.2.0
  json_annotation: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  freezed: ^2.3.2
  json_serializable: ^6.6.2

flutter:
  uses-material-design: true